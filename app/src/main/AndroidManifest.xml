<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.e.hospoclear">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HospoClear">
        <activity android:name=".ui.addNewHospital.login_doctor"></activity>
        <activity android:name=".ui.addNewHospital.personInformation" />
        <activity android:name=".ui.addNewHospital.register_OTP" />
        <activity android:name=".LoginUser.RegisterUser" />
        <activity android:name=".LoginUser.login_otp" />
        <activity android:name=".LoginUser.loginUser">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />
    </application>

</manifest>